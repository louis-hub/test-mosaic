{% extends 'base.html.twig' %}

{% block title %}
Login
{% endblock %}

{% block stylesheets %}
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('login') }}
{% endblock %}

{% block body %}
<div id="login" {% if error %} error="{{ error.messageKey | trans(error.messageData, 'security') }}" {% endif %}
    url="{{ path('login') }}" last-username="{{ last_username }}"></div>
{% endblock %}
